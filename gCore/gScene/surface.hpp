#ifndef SURFACE_HPP
#define SURFACE_HPP

namespace gfx {
 
    class surface {
    public:
        class settings {
                    settings();
        };
    
                surface( settings const& set = settings() );
                
    };
}

#endif